// export const maskNumber = (number) => {
//   return number.slice(-4).padStart(number.length, '*'); //replaces account number digits with * except for the last (4) digits of the account number
// };

//deleted use 3.23.21